// Generate random numbers from the normal distribution in python

# generate some integers
from random import randint

# Define a function 
def RandomNum(n):
    value = randint(0, n)
    return value
    
 #test the generator 
 print(RandomNum(10)
 
